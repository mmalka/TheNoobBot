itemName = GetItemInfo(22578);
for i = 1, GetItemCount(itemName)/10 do
     RunMacroText("/use " .. itemName);
end 
itemName = GetItemInfo(22577);
for i = 1, GetItemCount(itemName)/10 do
     RunMacroText("/use " .. itemName);
end 
itemName = GetItemInfo(22576);
for i = 1, GetItemCount(itemName)/10 do
     RunMacroText("/use " .. itemName);
end 
itemName = GetItemInfo(49655);
for i = 1, GetItemCount(itemName)/10 do
     RunMacroText("/use " .. itemName);
end 
itemName = GetItemInfo(22575);
for i = 1, GetItemCount(itemName)/10 do
     RunMacroText("/use " .. itemName);
end 
itemName = GetItemInfo(22574);
for i = 1, GetItemCount(itemName)/10 do
     RunMacroText("/use " .. itemName);
end 
itemName = GetItemInfo(22573);
for i = 1, GetItemCount(itemName)/10 do
     RunMacroText("/use " .. itemName);
end 
itemName = GetItemInfo(22572);
for i = 1, GetItemCount(itemName)/10 do
     RunMacroText("/use " .. itemName);
end 
itemName = GetItemInfo(37705);
for i = 1, GetItemCount(itemName)/10 do
     RunMacroText("/use " .. itemName);
end 
itemName = GetItemInfo(37703);
for i = 1, GetItemCount(itemName)/10 do
     RunMacroText("/use " .. itemName);
end 
itemName = GetItemInfo(37704);
for i = 1, GetItemCount(itemName)/10 do
     RunMacroText("/use " .. itemName);
end 
itemName = GetItemInfo(37702);
for i = 1, GetItemCount(itemName)/10 do
     RunMacroText("/use " .. itemName);
end 
itemName = GetItemInfo(37701);
for i = 1, GetItemCount(itemName)/10 do
     RunMacroText("/use " .. itemName);
end 
itemName = GetItemInfo(37700);
for i = 1, GetItemCount(itemName)/10 do
     RunMacroText("/use " .. itemName);
end 
itemName = GetItemInfo(89112);
for i = 1, GetItemCount(itemName)/10 do
     RunMacroText("/use " .. itemName);
end 
itemName = GetItemInfo(72201);
for i = 1, GetItemCount(itemName) do
     RunMacroText("/use " .. itemName);
end 
for i=108294, 108309 do 
    itemName = GetItemInfo(i);
    for i = 1, GetItemCount(itemName)/10 do
         RunMacroText("/use " .. itemName);
    end 
end 
for i=108318, 108365 do 
    itemName = GetItemInfo(i);
    for i = 1, GetItemCount(itemName)/10 do
         RunMacroText("/use " .. itemName);
    end 
end 
for i=97619, 97624 do 
    itemName = GetItemInfo(i);
    for i = 1, GetItemCount(itemName)/10 do
         RunMacroText("/use " .. itemName);
    end 
end 
for i=109623, 109629 do 
    itemName = GetItemInfo(i);
    for i = 1, GetItemCount(itemName)/10 do
         RunMacroText("/use " .. itemName);
    end 
end 
itemName = GetItemInfo(108391);
for i = 1, GetItemCount(itemName)/10 do
     RunMacroText("/use " .. itemName);
end 
itemName = GetItemInfo(108043);
for i = 1, GetItemCount(itemName)/10 do
     RunMacroText("/use " .. itemName);
end 
itemName = GetItemInfo(110610);
for i = 1, GetItemCount(itemName)/10 do
     RunMacroText("/use " .. itemName);
end 
itemName = GetItemInfo(112157);
for i = 1, GetItemCount(itemName)/10 do
     RunMacroText("/use " .. itemName);
end 
itemName = GetItemInfo(112156);
for i = 1, GetItemCount(itemName)/10 do
     RunMacroText("/use " .. itemName);
end 
itemName = GetItemInfo(112155);
for i = 1, GetItemCount(itemName)/10 do
     RunMacroText("/use " .. itemName);
end 
itemName = GetItemInfo(112158);
for i = 1, GetItemCount(itemName)/10 do
     RunMacroText("/use " .. itemName);
end 
itemName = GetItemInfo(112178);
for i = 1, GetItemCount(itemName)/10 do
     RunMacroText("/use " .. itemName);
end 
itemName = GetItemInfo(112177);
for i = 1, GetItemCount(itemName)/10 do
     RunMacroText("/use " .. itemName);
end 
itemName = GetItemInfo(112182);
for i = 1, GetItemCount(itemName)/10 do
     RunMacroText("/use " .. itemName);
end 
itemName = GetItemInfo(112179);
for i = 1, GetItemCount(itemName)/10 do
     RunMacroText("/use " .. itemName);
end 
itemName = GetItemInfo(112180);
for i = 1, GetItemCount(itemName)/10 do
     RunMacroText("/use " .. itemName);
end 
itemName = GetItemInfo(112181);
for i = 1, GetItemCount(itemName)/10 do
     RunMacroText("/use " .. itemName);
end 
itemName = GetItemInfo(112183);
for i = 1, GetItemCount(itemName)/10 do
     RunMacroText("/use " .. itemName);
end 
itemName = GetItemInfo(112184);
for i = 1, GetItemCount(itemName)/10 do
     RunMacroText("/use " .. itemName);
end 
itemName = GetItemInfo(112185);
for i = 1, GetItemCount(itemName)/10 do
     RunMacroText("/use " .. itemName);
end 
itemName = GetItemInfo(25649);
for i = 1, GetItemCount(itemName)/5 do
     RunMacroText("/use " .. itemName);
end 
itemName = GetItemInfo(33567);
for i = 1, GetItemCount(itemName)/5 do
     RunMacroText("/use " .. itemName);
end 
itemName = GetItemInfo(72162);
for i = 1, GetItemCount(itemName)/5 do
     RunMacroText("/use " .. itemName);
end 
itemName = GetItemInfo(52977);
for i = 1, GetItemCount(itemName)/5 do
     RunMacroText("/use " .. itemName);
end 
itemName = GetItemInfo(2934);
for i = 1, GetItemCount(itemName)/3 do
     RunMacroText("/use " .. itemName);
end 
itemName = GetItemInfo(129746);
for i = 1, GetItemCount(itemName) do
     RunMacroText("/use " .. itemName);
end  
itemName = GetItemInfo(140222);
for i = 1, GetItemCount(itemName) do
     RunMacroText("/use " .. itemName);
end  
itemName = GetItemInfo(140226);
for i = 1, GetItemCount(itemName) do
     RunMacroText("/use " .. itemName);
end 
itemName = GetItemInfo(61387);
for i = 1, GetItemCount(itemName) do
     RunMacroText("/use " .. itemName);
end 
 
